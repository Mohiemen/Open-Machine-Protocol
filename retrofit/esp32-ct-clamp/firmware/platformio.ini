; OMP esp32-ct-clamp firmware
; STATUS: DRAFT - written to the retrofit guide's spec, not yet validated on
; hardware. Compile with `pio run`, flash with `pio run -t upload`.
[env:esp32dev]
platform = espressif32
board = esp32dev            ; ESP32-WROOM-32 per hardware/BOM.md (not -C3)
framework = arduino
monitor_speed = 115200
lib_deps =
    knolleary/PubSubClient@^2.8
    bblanchon/ArduinoJson@^7.0
